# .envrc

# Option 1: Use default shell (both Python and Rust)
use flake

# Option 2: Use POC shell (Python only)
# use flake .#poc

# Option 3: Use Core shell (Rust only)
# use flake .#core

# Optional: Set up additional environment variables
export FLUX_ENV="development"
export RUST_LOG="debug"
export PYTHONDONTWRITEBYTECODE=1

# Optional: Auto-activate Python venv if it exists
if [ -d "poc/.venv" ]; then
  source poc/.venv/bin/activate
fi
